spring:
  application:
    name: mall-monitor
  profiles:
    active: dev
  security:
    user:
      name: macro
      password: 123456
  boot:
    admin:
      discovery:
        ignored-services: ${spring.application.name}
server:
  port: 8101
